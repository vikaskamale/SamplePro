import { TextonlyDirective } from './textonly.directive';

describe('TextonlyDirective', () => {
  it('should create an instance', () => {
  //  const directive = new TextonlyDirective();
  //  expect(directive).toBeTruthy();
  });
});
